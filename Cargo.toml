[package]
name = "diff-pretty"
version = "0.0.1"
edition = "2024"

[dev-dependencies]
divan = "0.1"

[[bench]]
name = "bench"
harness = false

[profile.dev]
debug = 0
strip = "debuginfo"

[profile.release]
lto = "fat"
opt-level = 3
codegen-units = 1
panic = "abort"
strip = true
